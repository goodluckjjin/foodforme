import React from 'react';
import './style.scss';

const Nav = (props) => {
    return (
        <nav className="nav">
            <a href="">My page</a>
            <a href="">Logout</a>
        </nav>
    );
}

export default Nav;